{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npx prisma generate && npx playwright install chromium --with-deps"
  },
  "deploy": {
    "startCommand": "npm run worker"
  }
}
